apiVersion: v1
kind: ReplicationController
metadata:
  name: nginx-rc
  labels:
    env: demo
# Spec of replication controller
spec:
  template:
    metadata:
      labels:
        env: demo
      name: nginx
    # Spec of pod template
    spec:
      containers:
        - image: nginx
          name: nginx
  replicas: 3
